# Accessory images

These SVG files are local on-brand placeholders for the Accessories page. Replace any file with your own product photography and update the matching `image` path in `src/data/accessories.js` if you change the filename or extension.
